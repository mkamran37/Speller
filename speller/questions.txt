0.  It is a lung disease caused by inhaling very fine ash and sand dust. Also, the longest word in this problem set.
1.  It returns the resource usage measures for its paramaeter "who"
2.  16
3.  Because its values are getting changed in that particular function and if we were to pass the parameters by value, they wouldn't be changed.
4.  Speller.c reads words from the file, character by character, using a for loop. It begins reading the first character of 
    the file, and incrementing by one character until it reaches the end of the file. If the character is a letter or apostrophe,
    it is added to the word array. If the word becomes larger than what we have defined as the largest word possible, the word is
    ignored. If the word has a digit, it is ignored as well.
    
    If a word still exists, it is counted, checked for spelling, and printed. The amount of time it takes to complete the
    checking is logged in time_check.
5.  To be able to account for numerical inputs and other punctuation which we have deemed invalid.
6.  From what I have learnt, "const" is used when we are passing the arguments, not by value, but by reference. Hence, here we have passed the parameters for load and check as const.
